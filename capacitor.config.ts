import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourname.novelist',
  appName: 'Novelist Workspace',
  webDir: 'dist'
};

export default config;
